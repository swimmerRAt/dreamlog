# Basic Android App

Minimal Android application skeleton.

## Structure

- `app/src/main/AndroidManifest.xml`: app manifest and launcher activity
- `app/src/main/java/com/example/basicandroidapp/MainActivity.java`: first screen
- `app/src/main/res/values/`: strings, colors, and theme resources

Open this folder in Android Studio and run the `app` configuration.
